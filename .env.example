# คัดลอกไฟล์นี้เป็น .env แล้วใส่ API key ของคุณ (ห้ามคอมมิตไฟล์ .env จริงขึ้น git)
OPENAI_API_KEY=sk-...
PORT=3001

# (ไม่บังคับ) ใส่ URL หน้าเว็บที่ deploy จริง เพื่อจำกัดว่ามีแค่โดเมนนี้เท่านั้นที่เรียก API ได้
# เช่น ALLOWED_ORIGIN=https://your-site.netlify.app
ALLOWED_ORIGIN=
